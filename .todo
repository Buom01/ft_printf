- unknow converter should show all expression and not just %
- put autopad to a higher level
- use a pointer to flags (and not a copy which is created when passing to funcs)
- padding of null char, should return multiple segment
- malloc and freeup
- norm
